# GitHub 代码上传说明

本次已整理好可公开的代码文件，尚未上传到 GitHub。请把 `open-hepatolithiasis-ssi-prediction` 文件夹内的内容上传到现有仓库根目录。

## 文件位置

本机目录：`D:\milk_dragon\medicine\开腹肝胆道结石手术SSI预测_新\github`

- `open-hepatolithiasis-ssi-prediction/`：需要上传的完整代码包，33 个文件，约 0.35 MB。
- `上传说明.md`：本说明，无需上传。
- `发布检查记录.md`：本地检查记录，无需上传。

不要把整个 SSI 项目、文章目录或私有数据复制到代码包里。包中只有两份“变量名—类型”CSV，没有患者数据。网页上传不会替你遵守 `.gitignore`，因此仅上传已整理的这个文件夹内部内容。

## 网页上传步骤

1. 登录有写入权限的 GitHub 账号，打开现有仓库：
   [sn-waterbird/open-hepatolithiasis-ssi-prediction](https://github.com/sn-waterbird/open-hepatolithiasis-ssi-prediction)。
2. 在仓库 **Code** 页面，确认分支为 `main`。点击文件列表上方 **Add file → Upload files**。
3. 在电脑上打开 `github\open-hepatolithiasis-ssi-prediction` 文件夹，选中其**内部所有文件和子文件夹**，拖入网页上传区域。包括 `README.md`、`.gitignore`、两个依赖文件，以及 `analysis`、`docs`、`results`、`tests` 等目录。不要只拖入外层同名文件夹，否则仓库会多套一层目录。
4. 检查网页待上传清单：`README.md` 应在根目录，训练代码应显示为 `analysis/最终训练.py` 等路径；已有的简短 README 会被完整说明替换。确认没有临床数据、患者名单或逐例预测文件。
5. 在 **Commit changes** 区域填写说明：`Add manuscript analysis code and corrected NRI/IDI workflow`。如果页面允许直接提交至 `main`，选择该选项，然后点击 **Commit changes**。如果仓库设置要求新分支，则按页面提示创建分支和 Pull Request，并合并后再继续下一步。
6. 上传后回到仓库首页，确认能看到 `analysis`、`docs`、`results`、`tests`，并打开 `analysis/reclassification.py` 确认内容可读。
7. 用未登录的浏览器窗口访问仓库地址，确认这些代码可以公开查看。**完成这一步后，文章中“代码已在公开 GitHub 仓库提供”的声明才与实际一致。**

本包文件数量和大小均低于网页上传限制（每次最多 100 个文件、每个文件最多 25 MiB）。操作依据：[GitHub 官方上传说明](https://docs.github.com/en/repositories/working-with-files/managing-files/adding-a-file-to-a-repository)。

## 上传后建议保存版本

上传成功后，可以在仓库的 **Releases → Draft a new release** 创建 `v1.0.0` 标签，指向上传后的 `main`，标题为 `Manuscript analysis code 2026-09-20`。说明可填写：

> Source code accompanying the manuscript, including the corrected final-model NRI/IDI comparison. Patient-level data and fixed patient assignments are not included.

这是可选的版本归档步骤，便于审稿人识别文章对应的代码版本。不要把患者数据作为 Release 附件上传。

## 关于复现和授权

- README 为英文，运行顺序、输入要求、软件版本和已知方法学限制见 `docs/REPRODUCIBILITY.md`。
- 公开包没有患者数据或固定分组名单，外部读者仅凭仓库不能重新得到全部论文结果，需要合规获得研究数据。
- 6 项模拟数据检查已通过；修正程序使用原始保存的预测结果重算后，与文章 NRI/IDI 完全一致。整个模型训练网格未在此次打包时重跑。
- 此次没有替作者选择 MIT、Apache 等许可。公开代码和授予复用许可是不同事项；如果希望允许他人按开源许可证使用，后续由全体代码权利人确定后添加 LICENSE。
- 当前文章的新版本为 `manuscript_final_20260920_v2.docx`；补充材料继续使用 `Supplementary_final_20260920.docx`。
