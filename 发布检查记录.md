# 代码包与正文修改检查记录

日期：2026-09-20

正文保留原稿，另存 `manuscript_final_20260920_v2.docx`。本轮修改 BMI 缺失率为 37/348（10.63%），修正摘要方法和结论中的 SHAP 数据集表述，并在方法中明确 278 例 DEV 的 SHAP 属于开发样本中的描述性解释，不构成独立验证。未追查或调整分组。补充材料无需修改。

未写入“因为验证集太小才选择开发集”这一未经确认的历史动机。摘要交代分析使用哪个数据集即可；方法中解释描述性用途及解释边界更准确。

文档已通过本机 Word 导出并检查 30 页排版，重点检查摘要、BMI 和 SHAP 方法修改页。其他图表和参考文献内容未改变。文章仍需处理此前复审报告中的其余事项；本轮不代表对投稿准备状态重新出具全部通过结论。

代码包共 33 个文件，约 0.35 MB。其中 18 个原项目代码文件按字节保留，并记录源文件哈希。修正后的 NRI/IDI 程序新增输入校验和命令行路径参数，计算方法与上一轮修正完全一致。

检查结果：

- 所有 Python 文件均通过语法检查。
- 6 项模拟数据测试通过，覆盖手算公式、交换比较方向、相同概率、可重复抽样、无效数据和逐例配对校验。
- 使用受控目录中的 70 例保存预测结果运行公开版程序，其 NRI/IDI 点估计、标准误、置信区间、p 值及上升下降计数与文章重算结果完全一致。
- 公开版补充图 S1 程序运行成功，生成 PNG 的 SHA-256 与文章使用的修正图片相同。
- 公开目录中的两份 CSV 仅含变量名和计算类型；不含患者行、ID、分组名单或逐例预测。
- 已检查公开文件中不存在本机绝对路径、常见密钥标记或患者数据文件。仅复制清单内的源码和说明，不复制缓存与运行输出。
- 原始环境版本来自保留的安装元数据；完整训练流程未重新执行，不宣称已完成全流程跨环境复现。

待用户完成：上传代码包并核实仓库公开可访问。正文中的“代码已公开”声明保留，但在实际上传完成之前尚不成立。
